-- this code has remained un-edited for a year
-- this is just here so I can commit a special commit for this occasion
CREATE TABLE cards (
	ID int AUTO_INCREMENT PRIMARY KEY NOT NULL,
	name varchar(30) NOT NULL
);
